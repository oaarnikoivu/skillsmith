# OpenAI API key used by @ai-sdk/openai (required for LLM generation).
OPENAI_API_KEY=your_openai_api_key_here

# Model selection is required per CLI invocation via --model.

# Optional base URL override (for proxy or compatible endpoints).
# OPENAI_BASE_URL=https://api.openai.com/v1

# Anthropic API key (used with --provider anthropic).
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional test/dev-only mock response. If set, bypasses network LLM call.
# OPENAPI_TO_SKILLMD_LLM_MOCK_RESPONSE=# Example Skill

# Optional test/dev-only ordered mock responses for multi-call flows (e.g. segmented mode).
# Must be a JSON array of strings.
# OPENAPI_TO_SKILLMD_LLM_MOCK_RESPONSES=["# Segment 1", "# Segment 2", "# Root"]

# Optional path override for provider/model user config (default is OS-specific config dir).
# OPENAPI_TO_SKILLMD_CONFIG_PATH=/absolute/path/to/config.json

# Optional comma-separated env var names to enforce exact secret leak checks in generated markdown.
# OPENAPI_TO_SKILLMD_SECRET_ENV_NAMES=OPENAI_API_KEY,ANTHROPIC_API_KEY,API_KEY,BEARER_TOKEN

# Optional default parallelism for segmented generation (used when --parallelism is not provided).
# OPENAPI_TO_SKILLMD_SEGMENT_PARALLELISM=3
